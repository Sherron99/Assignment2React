export const baseUrl = "http://127.0.0.1:8000/";
//export 关键字用于将变量、函数、类等从一个模块中导出，以便在其他模块中可以通过 import 引入并使用它们。
//而 const 关键字用于声明一个常量，这个常量的值在声明后不能被改变。
//换句话说，你不能通过重新赋值来改变这个变量的引用。但是，如果这个引用指向的是一个对象或数组，你可以修改该对象或数组的内容。